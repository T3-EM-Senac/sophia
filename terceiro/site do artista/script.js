// script.js — simples interatividade (pode expandir depois)
document.addEventListener('DOMContentLoaded', () => {
  // Futuro: carregar obras via JSON, adicionar player de áudio, etc.
  console.log('Site Villa-Lobos carregado — autoria: SOPHIA');
});
